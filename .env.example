# Copy this file to `.env` and fill in your real Instagram credentials.
# `.env` is git-ignored — your credentials never leave this machine.
IG_USERNAME=your_instagram_username
IG_PASSWORD=your_instagram_password
